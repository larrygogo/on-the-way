# Step 1: 横向卷轴 2.5D 基础

## 项目初始化

### 技术栈
- **TypeScript** + **原生 Canvas**（不使用 three.js）
- **Vite** 构建工具
- **全屏 Canvas**，自适应 DPR（Device Pixel Ratio）

### 项目结构

```
on-the-way/
├── package.json              # Vite + TypeScript 依赖配置
├── tsconfig.json             # TypeScript 编译配置
├── vite.config.ts            # Vite 构建配置
├── index.html                # 入口 HTML（全屏 Canvas）
├── src/
│   ├── main.ts               # 应用入口，初始化游戏
│   ├── style.css             # 全屏样式，DPR 自适应
│   └── game/
│       ├── Renderable.ts     # 可渲染接口
│       ├── Camera.ts          # 相机系统（横向卷轴跟随）
│       ├── Player.ts          # 玩家实体（位置、移动、渲染）
│       ├── GroundBand.ts      # 地面带（可走区域）
│       ├── Renderer.ts        # 渲染器（Canvas 上下文管理，按 y 排序）
│       └── Game.ts            # 主游戏循环（更新、渲染、输入处理）
```

## 修改/新增文件列表

### 新增文件
1. **src/game/Renderable.ts** - 可渲染接口
   - `getDepthKey()` - 获取深度键（用于排序）
   - `render()` - 渲染方法

2. **src/game/Camera.ts** - 相机系统
   - 横向卷轴相机系统
   - `worldToScreen()` - 世界坐标转屏幕坐标
   - `follow()` - 跟随目标
   - `update()` - 更新相机位置

3. **src/game/Player.ts** - 玩家实体
   - 玩家位置（x, y, z）
   - WASD 移动控制
   - `setWalkRect()` - 设置可走区域
   - `clampPosition()` - 限制在 walkRect 内
   - 渲染（带脚下阴影椭圆）

4. **src/game/GroundBand.ts** - 地面带系统
   - 绘制可走区域（矩形）
   - 显示上下边界线
   - 扁平风格配色
   - `getWalkRect()` - 获取可走区域矩形

5. **src/game/Renderer.ts** - 渲染器
   - DPR 自适应（检测 `devicePixelRatio`）
   - Canvas 上下文管理
   - 按 y 值排序渲染（从小到大，实现遮挡）
   - `render()` - 渲染所有可渲染对象

6. **src/game/Game.ts** - 主游戏循环
   - 初始化所有系统
   - 输入处理（WASD 键盘事件）
   - 游戏循环（`requestAnimationFrame`）
   - 更新和渲染协调

7. **src/main.ts** - 应用入口
   - 初始化 Canvas
   - 创建 Game 实例
   - 启动游戏循环

8. **src/style.css** - 全屏样式
   - Canvas 全屏显示
   - DPR 自适应样式

9. **index.html** - 入口 HTML
   - 全屏 Canvas 元素

## 核心功能说明

### 一、坐标系统
- **世界坐标**: `(x, y, z)`
  - `x`: 左右推进（横向卷轴方向）
  - `y`: 上下走位（深度轴）
  - `z`: 暂时不用（保留字段）
- **屏幕映射**: `sx = x - cameraX`, `sy = y - cameraY`
- **深度排序**: 使用 `y` 作为 depthKey，越靠下（y 值大）的实体越在前

### 二、相机系统
- **横向卷轴**: 相机主要跟随玩家 x（横向卷轴）
- **屏幕中心**: 玩家始终在屏幕中心（x 方向）
- **y 跟随**: y 可固定或轻微跟随
- **核心方法**:
  - `worldToScreen(x, y, z)` - 将世界坐标转换为屏幕坐标
  - `follow(x, y)` - 设置跟随目标
  - `update()` - 更新相机位置

### 三、地面带 GroundBand
- **可走区域**: 矩形区域（宽 2000，高 400，从 y=200 开始）
- **边界线**: 显示上边界线和下边界线
- **扁平风格**: 深灰色地面，灰色边界线
- **WalkRect**: 提供 `getWalkRect()` 方法，返回可走区域矩形

### 四、玩家实体 Player
- **位置**: `(x, y, z)`
- **移动控制**: WASD 键控制
  - W: 向上移动（y 减小）
  - A: 向左移动（x 减小）
  - S: 向下移动（y 增加）
  - D: 向右移动（x 增加）
- **移动限制**: 限制在 walkRect 内（clamp）
- **移动速度**: 2.5 单位/帧
- **渲染**: 简单几何形状（矩形身体 + 小矩形头部）+ 脚下阴影椭圆
- **深度键**: `getDepthKey()` 返回 `y`（越靠下越在前）

### 五、渲染器 Renderer
- **DPR 自适应**: 检测 `window.devicePixelRatio`，自动缩放 Canvas 尺寸
- **深度排序**: 按 y 值从小到大排序
  - 值小的先绘制（在后面）
  - 值大的后绘制（在前面）
  - 实现遮挡效果：越靠下（y 值大）的实体越在前
- **Canvas 管理**: 管理 Canvas 上下文，处理窗口大小变化

### 六、Renderable 接口
- **目的**: 所有需要在屏幕上渲染的对象都应实现此接口
- **方法**:
  - `getDepthKey()` - 获取深度键，用于排序渲染顺序
  - `render(ctx, camera)` - 渲染对象

## 核心函数位置

### worldToScreen()
**位置**: `src/game/Camera.ts` (约第 47-51 行)
```typescript
worldToScreen(x: number, y: number, z: number = 0): { sx: number; sy: number } {
  return {
    sx: x - this.cameraX,
    sy: y - this.cameraY
  };
}
```
- 将世界坐标转换为屏幕坐标
- `sx = x - cameraX`
- `sy = y - cameraY`

### camera update()
**位置**: `src/game/Camera.ts` (约第 36-46 行)
```typescript
update(): void {
  if (this.followX) {
    this.cameraX = this.followX - this.screenCenterX;
  }
  if (this.followY) {
    this.cameraY = this.followY - this.screenCenterY;
  }
}
```
- 更新相机位置
- 主要跟随玩家 x（横向卷轴）
- y 可固定或轻微跟随

### render()
**位置**: `src/game/GroundBand.ts` (约第 30-70 行)
```typescript
render(ctx: CanvasRenderingContext2D, camera: Camera): void {
  const screenRect = camera.worldToScreenRect(this.x, this.y, this.width, this.height);
  // 绘制地面带和边界线
}
```
- 绘制地面带（可走区域）
- 显示上边界线和下边界线
- 扁平风格配色

### render() - 排序和绘制
**位置**: `src/game/Renderer.ts` (约第 81-93 行)
```typescript
render(renderables: Renderable[], camera: Camera, debugDraw: boolean = false): void {
  // 按深度键排序（从小到大，y + sortOffset 值小的先绘制，值大的后绘制，实现遮挡）
  const sorted = [...renderables].sort((a, b) => a.getDepthKey() - b.getDepthKey());
  
  // 清空画布
  this.clear();
  
  // 依次渲染
  for (const renderable of sorted) {
    renderable.render(this.ctx, camera);
  }
}
```
- 按 y 值从小到大排序
- 越靠下（y 值大）的实体越后绘制，实现遮挡效果
- 依次渲染所有实体

### player update()
**位置**: `src/game/Player.ts` (约第 60-90 行)
```typescript
update(): void {
  let dx = 0;
  let dy = 0;
  
  // 根据按键计算移动方向
  if (this.keys.has('w') || this.keys.has('W')) dy -= 1;
  if (this.keys.has('a') || this.keys.has('A')) dx -= 1;
  if (this.keys.has('s') || this.keys.has('S')) dy += 1;
  if (this.keys.has('d') || this.keys.has('D')) dx += 1;
  
  // 归一化对角线移动
  if (dx !== 0 && dy !== 0) {
    dx *= 0.707;
    dy *= 0.707;
  }
  
  // 移动并限制在 walkRect 内
  this.x += dx * this.speed;
  this.y += dy * this.speed;
  this.clampPosition();
}
```
- WASD 移动控制
- 支持对角线移动（速度归一化）
- 限制在 walkRect 内（clamp）

## 如何验收

### 1. 页面全屏显示，Canvas 自适应 DPR
- ✅ Canvas 占满整个浏览器窗口
- ✅ 高 DPR 设备上显示清晰（无模糊）
- ✅ 窗口大小变化时 Canvas 自动调整

### 2. 横向卷轴功能
- ✅ 玩家左右移动时，相机跟随（x 方向）
- ✅ 场景左右卷动，横向卷轴效果
- ✅ 玩家始终在屏幕中心（x 方向）

### 3. 上下走位功能
- ✅ 玩家可以上下移动（y 方向）
- ✅ 相机 y 可固定或轻微跟随
- ✅ 玩家位置限制在 walkRect 内

### 4. 地面带显示
- ✅ 显示可走区域（矩形）
- ✅ 显示上边界线和下边界线
- ✅ 扁平风格配色（深灰色地面，灰色边界线）

### 5. WASD 键控制玩家移动
- ✅ W/A/S/D 键分别控制上/左/下/右移动
- ✅ 移动平滑流畅
- ✅ 支持对角线移动（速度归一化）
- ✅ 限制在 walkRect 内（clamp）

### 6. 遮挡正确（按 y 排序）
- ✅ 使用 `depthKey = y` 进行深度排序
- ✅ 越靠下（y 值大）的实体越在前
- ✅ 渲染顺序正确（后面的覆盖前面的）

### 7. 玩家脚下阴影
- ✅ 玩家脚下绘制阴影椭圆
- ✅ 阴影位置正确（在脚下）

### 8. 代码结构清晰，易于扩展
- ✅ `Renderable` 接口预留，便于后续添加怪物、物品等
- ✅ 深度排序接口已实现，可直接使用
- ✅ 模块化设计，职责清晰

## 测试步骤

1. **运行项目**:
   ```bash
   npm install
   npm run dev
   ```

2. **测试全屏显示**:
   - 打开浏览器，Canvas 应该占满整个窗口
   - 检查高 DPR 设备上是否清晰

3. **测试横向卷轴**:
   - 按 D 键向右移动，场景应该向左卷动
   - 按 A 键向左移动，场景应该向右卷动
   - 玩家应该始终在屏幕中心（x 方向）

4. **测试上下走位**:
   - 按 W 键向上移动
   - 按 S 键向下移动
   - 玩家应该限制在 walkRect 内

5. **测试地面带**:
   - 观察地面带是否正确显示
   - 检查上下边界线是否可见

6. **测试遮挡**:
   - 如果有多个实体，检查遮挡关系是否正确
   - 越靠下的实体应该覆盖越靠上的实体

## 按键说明

- **W**: 向上移动（y 减小）
- **A**: 向左移动（x 减小）
- **S**: 向下移动（y 增加）
- **D**: 向右移动（x 增加）
- **F1**: 切换调试绘制（如果已实现）

## 技术细节

- **坐标系统**: 世界坐标 `(x, y, z)`，z 暂时不用（保留字段）
- **屏幕映射**: `sx = x - cameraX`, `sy = y - cameraY`
- **深度排序**: 使用 `y` 作为 depthKey，越靠下越在前
- **横向卷轴**: 相机主要跟随玩家 x，实现左右卷动
- **DPR 自适应**: 检测 `window.devicePixelRatio`，自动缩放 Canvas 尺寸
- **移动平滑**: 使用速度和时间差实现平滑移动（速度：2.5 单位/帧）
- **移动限制**: 玩家位置限制在 walkRect 内（clamp）

## 注意事项

- 这是横向卷轴风格的 2.5D 实现，不是等距投影
- 深度排序基于 y 值，不是 x+y
- 相机主要跟随 x 方向，实现横向卷轴效果
- `Renderable` 接口为后续扩展预留，便于添加怪物、物品等实体
- 所有实体都应实现 `Renderable` 接口，并正确实现 `getDepthKey()` 方法
